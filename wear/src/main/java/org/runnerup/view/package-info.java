/**
 * Wear OS UI lives under the {@code view/} source directory but uses {@code
 * org.runnerup.features} as its Java package so it can share types with the phone app. This is
 * intentional; phone {@code StartFragment} and wear activities are not in the same module.
 */
package org.runnerup.features;
